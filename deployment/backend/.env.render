# Render Production Environment Variables
NODE_ENV=production
RENDER=true

# Chrome/Puppeteer Configuration
PUPPETEER_EXECUTABLE_PATH=/usr/bin/google-chrome-stable
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true

# Fallback to bundled Chromium if Chrome installation fails
# Comment out above lines and uncomment below to use bundled Chromium
# PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=false

# Other required environment variables (set in Render Dashboard)
# SUPABASE_URL=your_supabase_url
# SUPABASE_ANON_KEY=your_supabase_anon_key
# JWT_SECRET=your_jwt_secret
# PORT=10000